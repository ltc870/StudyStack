services:
  postgres:
    image: postgres:18
    container_name: studystack-postgres
    environment:
      POSTGRES_DB: studystack
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: devpassword
    ports:
      - "55432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql

volumes:
  postgres-data: